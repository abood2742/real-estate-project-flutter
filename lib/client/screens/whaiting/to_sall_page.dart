import 'package:flutter/material.dart';

class ToSallPage extends StatelessWidget {
  const ToSallPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:Text('لا يوجد عقارت بيع لم تنشر بعد'),
    );
  }
}