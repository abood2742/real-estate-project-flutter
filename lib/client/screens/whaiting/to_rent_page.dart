import 'package:flutter/material.dart';

class ToRentPage extends StatelessWidget {
  const ToRentPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('لا يوجد عقارت إيجار لم تنشر بعد'),),
    );
  }
}