import 'package:flutter/material.dart';

class ReservationRent extends StatelessWidget {
  const ReservationRent({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}