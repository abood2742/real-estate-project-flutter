import 'package:flutter/material.dart';

class ClientChangePasswordPage extends StatelessWidget {
  const ClientChangePasswordPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}